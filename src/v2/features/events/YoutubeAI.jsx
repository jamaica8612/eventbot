/* ============================================================
   당첨노트 v2 — YouTube 응모 도우미 (stub)
   ※ 단계 6b에서 EventBodyToggle의 실제 Gemini 배선으로 교체된다.
   ============================================================ */

// eslint-disable-next-line no-unused-vars
export function hasYoutubeHelper(ev) {
  return false; // 6b에서 실제 판정으로 교체
}

export function YoutubeAI() {
  return null;
}
