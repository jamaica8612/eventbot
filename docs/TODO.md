# TODO

작업은 작고 독립적인 단위로 진행합니다. 한 번에 완성형 앱을 만들지 않습니다.

## 완료한 작업

- [x] Vite + React 최소 앱 뼈대를 만든다.
- [x] 기본 이벤트 카드와 상태 버튼을 만든다.
- [x] README, HANDOFF, TODO, DECISIONS 문서를 만든다.
- [x] React JSX 렌더링을 위해 `vite.config.js`를 추가한다.
- [x] Wanted Design System 참고 스타일과 Pretendard 폰트를 적용한다.
- [x] 브라우저 새로고침 후에도 이벤트 상태가 남도록 localStorage 저장을 붙인다.
- [x] 새 이벤트를 직접 추가하는 최소 입력 폼을 만든다.
- [x] 직접 추가한 이벤트가 새로고침 후에도 남도록 localStorage 저장을 붙인다.
- [x] 직접 추가 기능을 자동 수집 중심 방향에 맞춰 제거한다.
- [x] 슈퍼투데이 인기 이벤트 1차 크롤러를 만든다.
- [x] 크롤링 결과를 웹앱 목록에 자동으로 합친다.
- [x] 크롤링 결과 카드에 원문 링크 열기 버튼을 붙인다.
- [x] 슈퍼투데이 크롤링 상태를 점검하고 인스타그램 이벤트를 제외한다.
- [x] 출석/출첵/체크인 이벤트를 제외한다.
- [x] 크롤링 결과에 원문 제목, 원문 링크, 플랫폼, 순위, 북마크 수를 저장한다.
- [x] DB로 옮기기 위한 이벤트 데이터 스키마를 문서로 확정한다.
- [x] Supabase 테이블 초안을 설계한다.
- [x] 크롤링 간격 정책을 문서화한다.
- [x] 참여 결과 상태를 `결과 미확인`, `당첨`, `미당첨`으로 관리한다.

## 다음 작업

- [x] 크롤러 저장 대상을 JSON에서 DB로 바꾸는 작업을 설계한다.
- [x] Supabase 연결용 환경 변수 이름을 `.env.example`에 남긴다.
- [x] Supabase 환경 변수가 있으면 크롤러가 DB에 upsert하도록 구현한다.
- [x] Supabase 환경 변수가 있으면 웹앱이 DB에서 이벤트를 읽도록 구현한다.
- [ ] Supabase 프로젝트를 만들고 `supabase/schema.sql`을 실행한다.
- [x] Supabase 연결 후 GitHub Actions 하루 3회 자동 실행을 설계한다.
- [x] DB 전환 시 참여 결과 상태를 Supabase에 저장하도록 연결한다.
- [ ] 모바일 화면을 실제 기기 폭 기준으로 확인한다.
- [ ] 이벤트 카드에서 꼭 필요한 정보만 남겼는지 검토한다.
- [ ] 상태값 `대기`, `나중에`, `참여함`, `제외`가 실제 사용 흐름에 맞는지 확인한다.
- [ ] PC에서 집에 와서 정리하는 화면 구조를 한 단계 확장한다.
- [ ] Vercel에 배포하고 모바일 실기기에서 확인한다.
- [ ] 배포 URL을 넓게 공유하기 전 접근 제한 또는 로그인 방식을 정한다.

## 나중 작업

- [x] 슈퍼투데이 최신/오늘마감 목록 페이지 접근 가능성을 다시 확인한다.
- [x] 무료 또는 저비용 DB 후보를 비교하고 Supabase 사용 여부를 확정한다.
- [ ] Figma에서 정확한 Wanted 토큰을 받을 수 있으면 CSS 토큰을 정교화한다.
- [ ] 유튜브 댓글 이벤트를 집에서 처리하기 위한 별도 보기 방식을 설계한다.
- [ ] 크롤러 실행 주기와 알림 기능을 웹앱과 분리된 작업으로 설계한다.
- [ ] 배포 후보를 비교한다.

## 하지 않기로 한 작업

- [ ] AI 댓글 생성 기능
- [ ] 복잡한 관리자 기능
- [ ] 모바일 앱과 PC 앱을 따로 만드는 작업
- [ ] 크롤러, 웹앱, 알림을 한 파일에 섞는 작업

## 2026-05-02 브라우저 코멘트 반영

- [x] 필터에 `전체보기`를 추가했다.
- [x] `전체보기`는 아직 처리하지 않은 `ready` 이벤트 전체로 정의했다.
- [x] `전체보기`에서 `나중에`, `참여함`, `제외`를 누르면 해당 이벤트가 현재 목록에서 빠지게 했다.
- [x] 상단 요약 카드(`현장 딸각`, `결과 미확인`, `당첨`)를 누르면 해당 분류로 이동하게 했다.
- [x] `원문 보기` 링크를 현재 탭에서 열도록 수정했다.
- [x] 카드 본문에 수집된 원문 기반 정보(원문 제목, 플랫폼, 저장 수, 목록 순위)를 표시했다.
- [x] 모바일에서 불편한 리스트박스 필터를 제거하고 하단 고정 메뉴로 바꿨다.
- [x] 자주 쓰는 분류는 하단 메뉴에 두고, 덜 자주 쓰는 분류는 목록 상단 칩으로 분리했다.
- [x] `원문 보기` 버튼을 제거하고 `참여하기` 버튼으로 바꿨다.
- [x] `참여하기`는 슈퍼투데이 `bbs/link.php` 응모 링크 형식으로 이동하게 했다.
- [x] 카드 안의 원문 정보는 기본 몇 줄만 보이고 누르면 펼쳐지도록 바꿨다.
- [x] `당첨`을 단순 카드 목록이 아니라 당첨 관리 장부 화면으로 바꿨다.
- [x] 당첨 이벤트에 금액과 수령상태(`미수령`, `수령요청`, `수령완료`)를 저장할 수 있게 했다.
- [x] 모바일은 당첨 카드형 장부, PC는 표형 장부에 가깝게 보이도록 했다.
- [x] 큰 설명형 상단 UI를 줄이고 `판단 대기`, `현장 딸각`, `결과 확인`, `당첨금` 중심의 컴팩트 대시보드로 바꿨다.
- [x] 하단 메뉴를 6개에서 5개로 줄이고 `참여함`은 상단 보조 칩으로 옮겼다.
- [x] PC 레이아웃도 왼쪽 고정 패널 느낌을 줄이고 한 화면에서 관리하기 쉽게 정리했다.
